venv/
__pycache__/
*.pyc
.DS_Store
log.lammps
log.cite
.claude/settings.local.json
*.egg-info/
build/
dist/
# Compiled MesoMem pair-style plugin (rebuilt on demand from the vendored
# cpp sources in lammps_live/forcefields/mesomem_ff/ -- see that package).
lammps_live/forcefields/mesomem_ff/*.dylib
lammps_live/forcefields/mesomem_ff/*.so
lammps_live/forcefields/mesomem_ff/*.dll
lammps_live/forcefields/mesomem_ff/*.build.json
lammps_live/forcefields/mesomem_ff/_obj/
lammps_live/forcefields/mesomem_ff/_mpi_stub/
# A personal config file (login, account, compiler flags) kept next to the
# checkout rather than in ~/.config -- see lammps_live/userconfig.py.
lammps-live.toml
# Trajectory dumps written into the cwd by a running scenario.
*.lammpstrj
# Generated by docs/impostor-book/build.py from the sources next to it.
docs/impostor-renderer.epub
# The collaborator's LAMMPS data files, written by the scripts next to them
# (spherical_vesicle_ds.py, then combine_vesicle_polymer.py) -- 18 MB of text
# that nothing in lammps_live/ reads; the playground builds the system itself.
polymer/input/
